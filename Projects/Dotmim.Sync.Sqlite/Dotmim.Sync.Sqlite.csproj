<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Sébastien Pertus</Authors>
    <Company>Microsoft</Company>
    <Title>Dotmim.Sync.Sqlite</Title>
    <Description>SQLite Sync Provider. Actually can be used as Client provider (Don't use it as a Server provider)</Description>
    <PackageProjectUrl>https://mimetis.github.io/Dotmim.Sync/</PackageProjectUrl>
    <PackageTags>Dotmim.Sync, SQLite Sync Core Provider, SQLite Sync, .Net Standard 2.0, Sync, Synchronization, Sync Framework</PackageTags>
    <Version>0.3.0.0</Version>
    <AssemblyVersion>0.3.0.0</AssemblyVersion>
    <Product>Dotmim.Sync.Sqlite OPTI-Q</Product>
    <PackageReleaseNotes>
      #0.3.0.0
      - allowed sqlite to use IsDirty column for change tracking
      - updated to Microsoft.Data.Sqlite 9.0.1
      - guids MUST now be stored as texts/strings and NOT as blobs
      #0.2.12.0
      - TableChangesApplyingEventArgs contains the canges view and sync context
      #0.2.11.0
      coreprovider.getstatefromdmrow greatly simplified,
      sqlite select_changes query only returns local changes and ignores "reinit" (it is the client db after all...)
    </PackageReleaseNotes>
  </PropertyGroup>

  <!--generate NUGET package including symbols ans source on each release build -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSource>True</IncludeSource>
    <IncludeSymbols>True</IncludeSymbols>
  </PropertyGroup>


  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SQLite">
      <Version>9.0.1</Version>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Dotmim.Sync.Core\Dotmim.Sync.Core.csproj" />
  </ItemGroup>

</Project>
